import { Access } from "@/features/experience/access";
export const metadata = { title: "Entra a tu cuenta de Eventos", robots: { index: false } };
export default function SignInPage() { return <Access mode="sign-in" />; }
