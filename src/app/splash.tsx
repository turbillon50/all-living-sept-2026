"use client";
import {useEffect,useState} from "react"; import {useRouter} from "next/navigation"; import {Ring,RING_MESSAGES} from "@/ui/ring";
export function Splash({target}:{target:string}){const router=useRouter();const[msg,setMsg]=useState("Cargando tu experiencia…");useEffect(()=>{router.prefetch(target);const reduce=matchMedia("(prefers-reduced-motion: reduce)").matches;const m=setTimeout(()=>setMsg(RING_MESSAGES[1] ?? "Conectando destinos…"),1050);const t=setTimeout(()=>router.replace(target),reduce?450:2600);return()=>{clearTimeout(m);clearTimeout(t)}},[router,target]);return <main className="premium-splash min-h-dvh pt-safe"><div className="ocean-depth" aria-hidden><span className="ray r1"/><span className="ray r2"/><span className="ray r3"/></div><div className="premium-brand"><div className="premium-name">ALL LIVING</div><div className="premium-tag">STAY · ENJOY · BELONG</div></div><Ring size={270} message={msg} label="Abriendo All Living"/><div className="premium-whisper">A MORE HUMAN WAY TO STAY</div></main>}
